BINDING = "0.0.0.0:8080"

DATABASE_URL = "postgres://postgres:azerty@localhost/speculare?sslmode=disable"

DATABASE_MAX_CONNECTION = 10	# (optional [default = 10])

KEY_PRIV = "key.pem"			# (optional, depends on HTTPS)
KEY_CERT = "cert.pem"			# (optional, depends on HTTPS)
HTTPS = true					# (optional [default = false])

API_TOKEN = "secret_password"	# (used to protect the ALERTS's API)

WSS_DOMAIN = "domain.xyz"		# Domain of SPECULRE-PGCDC

RUST_LOG = "info,alerts=info,actix_server=info,actix_web=info"	# (optional)

SMTP_HOST = "smtp.gmail.com"
SMTP_USER = "mail@gmail.com"
SMTP_PASSWORD = "password_super_secret"
SMTP_EMAIL_SENDER = "Speculare <alerts@speculare.cloud>"
SMTP_EMAIL_RECEIVER = "myemail@mail.com"